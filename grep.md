grep searches the input it is given and looks for content matching the pattern it has been given.

For example used in combination with a regex, it could be used to return lines beginning with 'A' inside a file. 
